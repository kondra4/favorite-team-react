import React from "react";

const HomePage = () => {
  // const Auth = useSelector((state) => state.persistStore.user.isAuth);
  // console.log(Auth);
  return (
    <div>
      On this site you can find your favorite football team and information
      about it
    </div>
  );
};

export default HomePage;
